services:
  frontend:
    build:
      context: ../frontend
      args:
        GATEWAY_BASE_URL: http://localhost:8080
    ports:
      - "3000:80"
    depends_on:
      - gateway

  gateway:
    build:
      context: ../gateway
    ports:
      - "8080:8080"
